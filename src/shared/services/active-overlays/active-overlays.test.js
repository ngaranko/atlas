import ActiveOverlays from './active-overlays'

describe('ActiveOverlays', () => {
  it('should return an array when calling getOverlays', () => {
    expect(ActiveOverlays.getOverlays()).toEqual([]);
  });

  it('should ', () => {
    
  });
});

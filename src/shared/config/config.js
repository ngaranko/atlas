import cmsConfig from './cms.config'

const {
  FOOTER: FOOTER_LINKS,
  HEADER: HEADER_LINKS,
  NAVIGATION: NAVIGATION_LINKS,
} = require('./content-links.json')

export { cmsConfig, FOOTER_LINKS, HEADER_LINKS, NAVIGATION_LINKS }
